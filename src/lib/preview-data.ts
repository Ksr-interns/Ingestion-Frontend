import type {
	Dataset,
	IngestionJob,
	Member,
	Organization,
	AuditEntry,
} from "@/types/platform";

export const previewDatasets: Dataset[] = [];
export const previewJobs: IngestionJob[] = [];
export const previewMembers: Member[] = [];
export const previewOrganizations: Organization[] = [];
export const previewAudit: AuditEntry[] = [];

