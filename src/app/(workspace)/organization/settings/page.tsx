import { SettingsPage } from "@/features/settings/components/SettingsPage";
export const metadata = { title: "Organization settings — Ingest" };
export default function Page() {
	return <SettingsPage organizationSettings />;
}
